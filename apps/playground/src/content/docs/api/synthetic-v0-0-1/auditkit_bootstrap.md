---
title: auditkit.bootstrap
description: "Assemble a module registry with all built-in scan modules registered."
version: "synthetic-v0.0.1"
versionLabel: "0.0.1 (legacy)"
---

:::caution[Older version]
You're viewing **0.0.1 (legacy)**. Latest is [0.1 (latest) →](/api/0-1-0/auditkit_bootstrap/).
:::


Assemble a module registry with all built-in scan modules registered.


#### load\_default\_registry

```python
def load_default_registry() -> ModuleRegistry
```

Create a registry and register every first-party scan module.

